# Meetha homepage

This homepage is split into focused modules so the conversion flow, image inventory, and product logic remain easy to audit. `homeData.ts` is the single source of truth for homepage imagery; each asset is intentionally used once.
